# Rust CLI

This directory contains the Rust OpenKit CLI runtime and command contracts.

## Commands

- `openkit memory init`
- `openkit memory doctor`
- `openkit memory capture`
- `openkit memory review`

## Notes

- This runtime is the target single-binary implementation for OpenKit.
- It is replacing the legacy Go CLI and bridge/sidecar model.
