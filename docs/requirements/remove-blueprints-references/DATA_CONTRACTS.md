# Data Contracts: Remove Blueprint References

No runtime data contracts are introduced or modified.

This change is documentation/content-only (skills/templates/scripts) and should not affect `managed.json` schemas or CLI flags.
